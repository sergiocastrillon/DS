(Acordarse de sustituir los test por los del campus por si al probar cosas borramos o cambiamos alguna cosa
de los test originales)


Versión alternativa del equals inspirada en la del hashCode

(Falta comprobar que implementación es más eficiente, esta o la implementación actual)

public boolean equals(Object o) {

        if (this == o) return true;
        if (o == null) return false;

        if(getClass() != o.getClass()) return false;
        Triangle t = (Triangle) o;


        int[] angles1 = {this.angle0,this.angle1,this.angle2};
        int[] angles2 = {t.angle0,t.angle1,t.angle2};
        Arrays.sort(angles1);
        Arrays.sort(angles2);
        return angles1[0]==angles2[0] && angles1[1]==angles2[1] && angles1[2] == angles2[2];
    }



